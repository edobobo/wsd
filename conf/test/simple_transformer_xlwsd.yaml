# module info
module_class: src.pl_modules.simple_transformer.SimpleTransformerPLModule
module_ckpt: null

device: 0

framework_dir: data/xl-wsd
raganato_path: ${framework_dir}/evaluation_datasets/test-${oc.env:XL_WSD_LANGUAGE}/test-${oc.env:XL_WSD_LANGUAGE}
gold_keys_path: ${raganato_path}.gold.key.txt

sense_inventory:
  _target_: src.sense_inventories.BabelNetSenseInventory
  inventory_path: ${framework_dir}/inventories/inventory.${oc.env:XL_WSD_LANGUAGE}.txt

predictor:
  _target_: src.predictors.simple_transformer.SimpleTransformerPredictor
  module: null
  tokenizer: null
  sense_inventory: ${sense_inventory}
  sense_vocabulary_path: null
  cut_senses: True
  tokens_per_batch: 4096

disambiguation_corpus:
  _target_: src.disambiguation_corpora.WordNetCorpus
  raganato_path: ${raganato_path}
  materialize: False
  cached: False
  shuffle: False
  is_doc_based: True
  is_train: False
